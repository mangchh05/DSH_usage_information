# DeepSeek 用量监控插件 — 保存 / 迁移 / 使用说明

一个给 **DSH Desktop** 用的便携插件：托盘实时余额、低余额充值提醒（点击直达充值页）、
单日用量超速提醒、`/deepseek-usage` 命令。**整包即拷即用，不写死任何本机路径。**

---

## 一、这个包里有什么

```
dsh-deepseek-usage-portable/
├── install.ps1              ← 一键安装脚本（新电脑/新用户直接运行）
├── dsh-deepseek-usage/      ← 插件本体（全部代码都在这）
│   ├── package.json
│   ├── cordis.patch.yml
│   └── lib/index.js
└── 使用说明.md              ← 本文件
```

插件本体零外部依赖（只用 DSH 自带的 `@deepseek-ai/*` 核心包），状态自动写入
`<DSH主目录>\dsh-deepseek-usage\`，所以**换机/换用户不会残留任何路径引用**。

---

## 二、保存（备份/带走）

**只需保存这一个文件夹**：`dsh-deepseek-usage-portable/`
（拷到 U 盘、网盘、Git 仓库都可以。zip 版直接解压即可。）

> 插件不包含你的 API Key。换机后需要在新机器的 DSH 里**重新填写密钥**（见下），
> 或者把旧机的 `C:\Users\<你>\.dsh\.credentials.yaml` 一并拷过去（这是 DSH 的密钥文件）。

---

## 三、新电脑 / 新用户安装步骤

前置：目标机器已安装并运行过一次 **DSH Desktop**（这样才有 `~\.dsh\profiles\<profile>`）。

1. 把 `dsh-deepseek-usage-portable` 文件夹解压到任意位置；
2. 打开 PowerShell，运行：
   ```powershell
   pwsh -File 解压路径\install.ps1
   ```
   - 脚本会自动识别你 DSH Desktop 正在用的 profile（`desktop` 或 `web`）；
   - 也可以手动指定：`pwsh -File install.ps1 -ProfileName desktop`
   - 或指定主目录：`pwsh -File install.ps1 -DshHome "D:\MyDSH"`
3. **彻底退出并重开 DSH Desktop**（托盘菜单 → 退出，不是关窗口）。

> 没有 pwsh？Windows 自带 PowerShell，可先用 `powershell -ExecutionPolicy Bypass -File 解压路径\install.ps1`。

---

## 四、新机器上需要重新配置的东西

| 项目 | 说明 |
| --- | --- |
| **API Key（必须）** | DSH 设置 → 模型（Models）页填写 `DEEPSEEK_API_KEY`（与你平时用 DeepSeek 模型的是同一个） |
| 阈值 / 轮询间隔（可选） | 设置 → `deepseek-usage`：默认低余额 5 元、单日 30 元、每 5 分钟轮询 |

---

## 五、日常使用

- **托盘**：右键 DSH 图标 → `DeepSeek 余额 ¥… · 今日 ¥…`；左键=立即刷新；右键=详情 + 立即刷新 + 前往充值。
- **余额 < 5 元**：弹窗提醒，**点击通知直接打开** `https://platform.deepseek.com/top_up` 充值页。
- **单日用量 > 30 元**：弹窗提醒用量过快。
- **对话里**：输入 `/deepseek-usage` 立即查看余额/今日用量明细。

---

## 六、更新 / 卸载 / 排障

- **更新**：用新版覆盖 `dsh-deepseek-usage` 文件夹内容，重新跑 `install.ps1`，重启即可。
- **卸载**：删除 `C:\Users\<你>\.dsh\profiles\<profile>\node_modules\dsh-deepseek-usage`，
  并从该 profile 的 `cordis.patch.yml` 删掉 `- id: dsh-deepseek-usage` 那一段，重启。
- **托盘显示“余额未知”**：日志 `%APPDATA%\DSH Desktop\logs\dsh-*.log` 里找
  `dsh-deepseek-usage` 的报错；最常见原因是密钥未填或失效（去设置→模型页检查）。
- **当日用量是估算值**（平台无按日账单接口，按余额差值估算）；精确账单以平台控制台为准。
